---
all:
  children:
    rpi:
      hosts:
        pi0:
          ansible_host: 192.168.68.10
        pi1:
          ansible_host: 192.168.68.11
    storage_server:
      hosts:
        pi0:
    nfs_clients:
      hosts:
        pi1:
    # Hosts running the Prometheus/Grafana/Alertmanager stack. Group
    # membership — not a hardcoded hostname — decides where the
    # alertmanager config and its bot token get deployed, so relocating
    # the monitoring stack is an inventory change.
    monitoring:
      hosts:
        pi1:
