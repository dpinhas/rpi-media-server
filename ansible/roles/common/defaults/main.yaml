---
common_packages:
  - vim
  - git
  - libffi-dev
  - libssl-dev
  - python3-dev
  - python3
  - python3-pip
  - nfs-common
  - needrestart

# pi_locale is set in inventory/group_vars/all/vars.yaml, which outranks role
# defaults; a copy here would never be used.
